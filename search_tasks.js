function filterTasks() {
    let search = document.getElementById('searchInput').value;
    search = search.toLowerCase();
    // document.getElementById('card-container').innerHTML = /*html*/``;

    // for (let index = 0; index < pricesAndArticels.length; index++) {
    //     if (pricesAndArticels[index]['articel'].toLowerCase().includes(search)) {
    //         articelSectionChanged(index);
    //         buildMenu(index);
    //     }
    // }
}